{
  "name": "gopherforge-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "test": "vitepress build && node --test scripts/*.test.mjs",
    "test:browser": "vitepress build && node scripts/browser-smoke.mjs",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "node-html-parser": "^9.0.1",
    "vitepress": "^1.6.3"
  },
  "overrides": {
    "vite": "6.4.3"
  }
}
