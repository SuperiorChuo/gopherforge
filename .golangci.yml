run:
  timeout: 5m
  tests: true

issues:
  exclude-dirs:
    - server/logs
    - tdesign-vue-go

linters:
  enable:
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused

linters-settings:
  misspell:
    locale: US
