global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: go-admin-kit-backend
    metrics_path: /api/v1/metrics
    static_configs:
      - targets:
          - go-admin-kit-monitor:8081  # monitor 服务承载 /api/v1/metrics（原 backend 命名已不存在）
